/**
 * Frontend entry point — CSS only.
 *
 * State management and inter-block communication have been migrated to the
 * WordPress Interactivity API (src/interactivity/view.js). The old
 * PostsGrid and PostsFilter classes are no longer instantiated here.
 */

import './style.scss';
